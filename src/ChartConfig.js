import {Box, Button, Select, Text, TextInput} from "grommet";
import React, {useState} from "react";

export default () => {
    return (
        <Box pad={{top: 'small'}}>
            yeah, the chart config goes here
        </Box>
    )
}
